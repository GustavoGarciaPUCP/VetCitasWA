@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // forceLoad: true para que la petición pase por el servidor (cookie / challenge)
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}
